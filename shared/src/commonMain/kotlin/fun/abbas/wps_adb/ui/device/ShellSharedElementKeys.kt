package `fun`.abbas.wps_adb.ui.device

object ShellSharedElementKeys {
    fun hero(deviceId: String): String = "shell-hero-$deviceId"

    fun title(deviceId: String): String = "shell-title-$deviceId"
}
